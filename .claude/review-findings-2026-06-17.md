## #0 [medium/contained] relay-providers
**A bad/expired OpenAI key never surfaces to the dictation user — every dictation silently fails with a generic 'lost connection' message**
- files: /Users/macmini/dev/voice/src/providers/openai.js:85-89, /Users/macmini/dev/voice/public/dictation.js:199-257, /Users/macmini/dev/voice/public/dictation.js:834-838, /Users/macmini/dev/voice/scripts/parity/dictation-flow.test.js:319-327, /Users/macmini/dev/voice/docs/RELAY_PROTOCOL.md:35
- impact: An OpenAI user with a wrong or expired key gets a vague 'lost the connection' error on EVERY dictation, with no hint that the key is the problem — a frustrating dead-end. RELAY_PROTOCOL.md:35 documents `local.error` as the frame for 'Any upstream failure', yet an auth-driven upstream close is delivered as `closed`, so the contract is also not honored for this failure class. Deepgram and whisper-local are unaffected (Deepgram's missing-key check at realtime-relay.js:95-98 and bad-key handshake-4xx both DO emit local.error).
- fix: Prefer the renderer-side fix to keep the wire contract intact: add a branch in dictation.js handleRealtimeEvent for `t === "local.status" && msg.status === "closed"` that, when no terminal/transcript frame has been seen for this utterance, calls reportFailure with the close reason — e.g. reportFailure("Transcription failed — check your OpenAI API key. (" + (msg.reason || ("closed " + msg.code)) + ")"). Guard it so a NORMAL close (code 1000 / empty reason / a session that already finalized via alreadyFinalized or gotTerminalEvent) does NOT trigger a spurious failure on every successful dictation. The relay-side alternative (synthesize local.error in openai.js close handler) is smaller but must apply the same normal-close guard (code !== 1000 with a non-empty reason on a session that produced no transcript) for the same reason. Either way the change is on the live dictation failure path, so the normal-close guard is the load-bearing detail.

## #1 [medium/contained] resource-lifecycle
**ensureWhisperServer returns the cached ready-promise regardless of the requested model, so a server warmed for one model can be reused for a different model**
- files: /Users/macmini/dev/voice/src/providers/whisper-local.js:284-285, /Users/macmini/dev/voice/src/benchmark-run.js:73-74
- impact: In the narrow window where a benchmark server is left running (the leak above) and the model in use changes without a stopWhisperServer() in between, dictation would transcribe with the wrong/old model until the server is restarted. Today's call sites mostly avoid it, so impact is limited.
- fix: In ensureWhisperServer, remember the resolved model path the current server was started with (e.g. a module-level whisperServerModel set at whisper-local.js:295). At the top (line 285), when whisperServerReady is cached AND the newly resolved model differs from whisperServerModel, call stopWhisperServer() and fall through to respawn instead of returning the stale promise. This is the single chokepoint — correct-by-construction for all callers (dictation attach and benchmark alike) and mirrors benchmark-run.js's existing stop-then-warm pattern. Reset whisperServerModel = null in stopWhisperServer (alongside whisperServerProc/whisperServerReady at lines 274-275). The lighter alternative (stopWhisperServer() inside applyEnvPatchLive when WHISPER_MODEL changed) works for today's UI but misses future callers, so the chokepoint fix is preferred. One comparison plus reuse of existing teardown — contained.

## #2 [medium/contained] security-ipc
**Relay accepts any cross-origin WebSocket and proxies the user's OpenAI key — no Origin check**
- files: realtime-relay.js:68-81, realtime-relay.js:83-121, src/providers/openai.js:25-33, src/providers/openai.js:93-100, server.js:88-91
- impact: Any website the user visits while GVoice is running can connect to the loopback relay and burn the user's OpenAI Realtime credits (and Deepgram, via ?provider=deepgram) — billing abuse and data exfiltration of whatever audio the page can capture. The relay's entire reason for existing is to keep the API key out of untrusted code (docs: it 'spends the API keys'); the loopback bind (intentional decision #5) stops the LAN but not a local browser tab reaching localhost. The port is random but trivially port-scannable from a malicious page.
- fix: Add a per-launch shared-secret token (browser-independent, the strongest control). main.js generates a random token at boot, injects it into the dictation-window URL (main.js:787 / reloadDictationWindow:1713) and passes it as a relay option; public/dictation.js forwards it on the ws URL (dictation.js:167-171); the `upgrade` handler (realtime-relay.js:68-81) calls `socket.destroy()` when the token query param is absent or mismatched, before handleUpgrade. Lead with the token rather than an Origin allow-list because: the reusable web component (realtime-voice-agent.js:52-56) is designed for cross-origin embedding via a configurable `endpoint`, so a localhost-only Origin allow-list would break legitimate use; and the "allow when Origin is absent" clause in the original proposal is both a bypass and factually wrong — the real dictation renderer loads from http://127.0.0.1:PORT (main.js:787), so it always sends Origin: http://127.0.0.1:PORT and is never Origin-less. An Origin allow-list can be a secondary defense-in-depth layer but not the primary gate. Contained blast radius with one sharp edge: the check sits on the connection-acceptance path, so the token MUST be plumbed to the legitimate renderer in the same change or all dictation (the app's core function) breaks.

## #3 [medium/contained] crossplatform-ffi
**Synchronous wmic/PowerShell GPU probe freezes the entire Electron main process (and the Windows hotkey poll) for up to ~10s**
- files: src/hardware.js:81-110, src/hardware.js:118-127, main.js:1283-1294, main.js:1305-1307
- impact: When the user opens the Settings 'speech engine' panel on Windows, the main process blocks while wmic/PowerShell run. wmic was removed by default on recent Windows 11 builds (24H2+), so the fallback PowerShell+CIM path (cold-start ~1-6s) is increasingly the common case. During that block the whole event loop is frozen: the Windows hotkey is a `setInterval` poll in this same process (src/hotkey.js:129), so push-to-talk, the pill, and all IPC stop responding for up to ~10s.
- fix: Make detectGpu()/probeCapability() async using node:child_process execFile (promisified) with the same timeouts, and make the two IPC handlers await them: change main.js:1283 to `ipcMain.handle("engine:probe", async () => { const probe = await probeCapability(); ... })` and main.js:1305 to `const probe = await probeCapability();` (reuse the one probe for recommendedAssets at 1306-1307 rather than calling twice). The .Name in detectGpu and downstream classifyCapability stay synchronous/pure. This keeps the main event loop — and the Windows hotkey poll — responsive during the GPU probe. Touches only the engine-probe path; no impact on relay lifecycle, FFI, or paste timing.

## #4 [medium/trivial] simplification-deadcode
**.env.example ships CLEANUP_ENABLED=false, which disables the out-of-the-box cleanup the app and docs promise is on by default**
- files: .env.example:15, main.js:944, src/settings.js:139, SETUP.md:79
- impact: A developer who does the natural thing — copy .env.example to .env and fill in keys — silently turns OFF the cleanup pass, the opposite of the documented default and the whole zero-setup-cleanup design. They get raw, unpunctuated Whisper/Deepgram text pasted and may think cleanup is broken.
- fix: Change .env.example:15 from `CLEANUP_ENABLED=false` to `CLEANUP_ENABLED=true`. That matches the code default (main.js:944), the Settings default (settings.js:139), the documented default (SETUP.md:79), and the shipped-free-Groq-key intent (decision #7, SETUP.md:26). It is also consistent with the file's existing style, since the other booleans there (RECORDINGS_ENABLED=true at .env.example:23) are uncommented and set to their documented defaults. Alternatively comment the line out (`# CLEANUP_ENABLED=true`) so the example never overrides the code default. One-line change to a sample config file; no runtime code touched.

## #5 [medium/trivial] docs-accuracy
**WHISPER_LANGUAGE is a core user-facing env var but is missing from both settings tables**
- files: SETUP.md:69-86, docs/ARCHITECTURE.md:123-144, main.js:811, main.js:814, src/providers/whisper-local.js:474, src/settings.js:168, .env.example:5
- impact: The two tables that claim to be the complete list of settings omit the single env var that controls dictation language for every engine — the thing the right-Ctrl toggle and the Settings dropdown both write. A reader auditing the .env from the tables can't tell what `WHISPER_LANGUAGE=hr` does or that `auto` is the default.
- fix: Add a WHISPER_LANGUAGE row to BOTH the SETUP.md settings table (after WHISPER_PORT, ~line 78) and the ARCHITECTURE.md env table (~line 133). Suggested copy, with default `auto` and accurate "used by" scope — note it is NOT all engines (OpenAI's realtime provider does not read it; only whisper-local and Deepgram do): `WHISPER_LANGUAGE | auto | Dictation language for local Whisper and Deepgram: auto, hr, or en. The right-Ctrl tap and the Settings dropdown both write it; auto-detect is unreliable for short Croidatian so forcing hr makes it stick.` Do not over-claim "all engines" as the original proposed fix did.

## #6 [medium/trivial] docs-accuracy
**SETUP 'Languages' overstates auto-detect: the default whisper-server path constrains 'auto' to Croatian+English only**
- files: SETUP.md:104-106, src/providers/whisper-local.js:525, src/providers/whisper-local.js:528-529, main.js:794
- impact: A user who reads this and speaks French/Spanish/German expecting auto-detection on the default (server) engine gets it forced into Croatian or English — exactly the wrong-language hallucination the code comment (whisper-local.js:523-524) was written to prevent. The 'switch mid-sentence across 57 languages' promise is true only on the rarely-used CLI fallback.
- fix: Reword SETUP.md:106. Keep the true part — Whisper supports 50+ languages and you can dictate any one of them by setting WHISPER_LANGUAGE=<code> (e.g. fr, de, es), which works on the default fast on-device engine. Correct the false part — automatic detection (the "auto" toggle) is constrained to Croatian + English only, to prevent wrong-language hallucinations; it does not free-detect across all languages or switch arbitrarily mid-sentence. Do NOT touch the hr/en constraint code (lines 525/528-529/651-655) — that constraint is the intentional anti-hallucination design; only the doc sentence is wrong.

## #7 [low/trivial] dictation-core
**Renderer's two terminal guards (alreadyFinalized vs failureHandled) don't check each other — a late error frame can paste-after-error or error-after-paste**
- files: /Users/macmini/dev/voice/public/dictation.js:250-256, /Users/macmini/dev/voice/public/dictation.js:280-294, /Users/macmini/dev/voice/public/dictation.js:296-307, /Users/macmini/dev/voice/src/providers/deepgram.js:140-150
- impact: Two reachable orderings, both user-visible. (a) `completed` fires → finalizeAndSend sends the transcript → main types it → a trailing `local.error` then calls reportFailure → a second terminal IPC (dictation:error / dictation:failure) repaints a red error pill over a dictation that actually typed fine. (b) Worse: `local.error` arrives first (failureHandled=true, user sees 'couldn't transcribe'), then a delayed `completed` arrives → finalizeAndSend sees alreadyFinalized===false → sends a transcript → main types text into the focused field after the user already gave up. This is the classic 'transcript pasted after teardown / out-of-order' case. Windows is narrow but real on flaky networks/single-leg manual hr/en.
- fix: One-line guard on the renderer's error branch, mirroring the failureTimer callback at line 859. In public/dictation.js before line 251, add: `if (alreadyFinalized || gotTerminalEvent || failureHandled) return;` at the top of the `if (t === "error" || t === "local.error")` block. This makes "at most one terminal IPC per utterance" hold regardless of frame order and matches an existing pattern already in the file, so it is behavior-narrowing and safe. A shared terminalSent flag is unnecessary — the three existing flags already cover both paths once the error branch checks them. No relay change is required for the confirmed ordering (a); optionally add a completedSent guard before deepgram.js line 149 for defense-in-depth, but that is not load-bearing for the user-visible fix.

## #8 [low/contained] dictation-core
**Worklet never force-flushes the partial buffer at end of hold — last ~85ms of the tail-drain audio is dropped**
- files: /Users/macmini/dev/voice/public/audio-capture-worklet.js:36-46, /Users/macmini/dev/voice/public/audio-capture-worklet.js:48-59, /Users/macmini/dev/voice/public/dictation.js:429
- impact: Up to ~85ms at the very end of the 450ms post-release tail is lost. Because decision #1 deliberately extended the tail to 450ms specifically to over-cover trailing speech, the lost slice falls ~365-450ms after key release — by which point the speaker has essentially always stopped. Real speech impact is near zero; this is a tidiness/correctness note, not a clipping bug.
- fix: Leave as-is is the honest recommendation. The proposed fix as written does not work: posting a one-shot 'flush' message from finishUtterance is async — by the time the worklet posts the partial back, draining is already false (line 786) and isRecording is false, so the onmessage guard at line 429 drops it; and the commit (line 833) has already been sent, so even if it passed the guard it would arrive too late for the engine. A correct fix would have to defer both the draining=false flip AND the commit until a flush-ack round-trips from the worklet (callback/async-driven commit sequencing) — that reorders the load-bearing commit lifecycle for a slice (~42ms avg) that sits 400ms+ after key release, in silence, and is already over-covered by the deliberate 450ms tail (decision #1). Net: document it as a conscious choice; do not change commit timing for near-zero real-speech benefit.

## #9 [low/trivial] dictation-core
**dictation:error and dictation:mic-warning IPC handlers call done() without finalize(), leaving the 25s safety timer armed**
- files: /Users/macmini/dev/voice/main.js:1202-1209, /Users/macmini/dev/voice/main.js:1377-1386, /Users/macmini/dev/voice/src/dictation-session.js:60-71
- impact: Benign in practice: the dangling timer fires up to 25s later, checks `if (this.busy)` and no-ops because done() already set busy=false (dictation-session.js line 65); the next tryStart() also clears it via _clearSafetyTimer(). No stuck-busy, no double-fire. Only a stray, harmless timer lingers. Worth a one-line fix for symmetry and to keep the lifecycle contract (finalize before done) uniform across all terminal handlers.
- fix: Add dictation.finalize() immediately before dictation.done() in both the dictation:error handler (main.js:1203) and the dictation:mic-warning handler (main.js:1378), matching the transcript and failure handlers. This clears the armed 25s safety timer on those terminal paths so the lifecycle contract (finalize before done) is uniform. One line added per handler; no behavior change beyond eliminating the harmless lingering timer.

## #10 [low/trivial] relay-providers
**Deepgram emits the 'connected' status frame once per leg — twice in auto/multi-language mode**
- files: /Users/macmini/dev/voice/src/providers/deepgram.js:84-86, /Users/macmini/dev/voice/docs/RELAY_PROTOCOL.md:33
- impact: Harmless in practice: the dictation renderer ignores local.status entirely, and the parity invariant only requires connected-before-transcription, not exactly-once (dictation-flow.test.js:116-130). The web voice agent would just set 'Connected' twice. Pure cosmetic/protocol-tidiness issue.
- fix: Add a shared `let connectedSent = false;` next to `completedSent` (deepgram.js:33), then in the open handler gate the emit: `if (!connectedSent) { connectedSent = true; sendToClient(clientSocket, { type: "local.status", status: "connected", provider: "deepgram", model }); }`. Keep the per-leg console.error log (line 85) unchanged for diagnosability. This mirrors the existing completedSent pattern, is confined to the open handler, and touches none of the leg lifecycle/Finalize/completion logic. Low priority — cosmetic only.

## #11 [low/contained] relay-providers
**Losing Deepgram leg is never proactively closed after completion — both legs stay connected to Deepgram between dictations**
- files: /Users/macmini/dev/voice/src/providers/deepgram.js:182-212, /Users/macmini/dev/voice/src/providers/deepgram.js:262-269, /Users/macmini/dev/voice/public/dictation.js:148-152
- impact: Two live Deepgram streaming connections linger between every dictation instead of one or zero. On loopback with no audio flowing it costs little, but it holds two upstream sockets open per idle session and the losing leg in particular serves no further purpose once emitCompleted has fired. Minor resource/cost note, not a correctness bug.
- fix: Optional and flag-only. The minimal correct version is to close only the non-winning leg(s) at the end of emitCompleted: after picking `best`, iterate legs and for each leg !== best send {type:"CloseStream"} then close its dgSocket. Do NOT proactively close the winning leg, because the dgSocket.on("close") handler (deepgram.js:161-164) calls clientSocket.close() once ALL legs are closed — closing every leg here would force the client socket shut immediately after completion instead of at the next press, a relay-lifecycle behavior change. Leaving the winner open preserves current lifecycle and reclaims only the genuinely useless losing leg. Given low payoff and the next press already replacing the socket, leaving it as-is is also defensible.

## #12 [low/trivial] relay-providers
**Deepgram treats malformed/non-JSON client frames as raw binary audio via message.toString() — would corrupt a true binary frame**
- files: /Users/macmini/dev/voice/src/providers/deepgram.js:214-220, /Users/macmini/dev/voice/docs/RELAY_PROTOCOL.md:23
- impact: Latent only: the GVoice dictation renderer always sends base64 inside JSON `input_audio_buffer.append` frames (dictation.js:439-444, 749-754) and never raw binary, so this path is not exercised by the shipped client. It would only bite a future/third-party client that sends raw binary audio per the documented contract.
- fix: Branch on frame type before stringifying. In deepgram.js:214-220, if the incoming message is not a string (i.e. it is a Buffer/binary frame), forward it unchanged and return before attempting toString()/JSON.parse — e.g. `clientSocket.on("message", (message, isBinary) => { if (isBinary) { forwardBinary(message); return; } const payload = message.toString(); ... })` (the ws message handler's second arg isBinary is the precise signal; alternatively check Buffer.isBuffer(message) && the frame opcode). forwardBinary already accepts a Buffer (deepgram.js:252), so no further change is needed. If raw-binary input is NOT a contract anyone intends to support, the equally valid and even cheaper fix is to drop the 'raw binary frame ... forwarded as-is' row at RELAY_PROTOCOL.md:23 so the doc matches the JSON-only reality. Either is a few lines; pick the doc-only fix unless a third-party binary client is actually expected.

## #13 [low/trivial] relay-providers
**Verified clean: no frames-after-close, no infinite renderer hang, backpressure not a concern**
- files: /Users/macmini/dev/voice/src/providers/_shared.js:59-63, /Users/macmini/dev/voice/public/dictation.js:842-862, /Users/macmini/dev/voice/src/providers/whisper-local.js:448-489
- impact: Documentation of due-diligence: these three commonly-flagged areas are sound and should not be 'fixed'. Inventing changes here would add risk to load-bearing relay/timer logic for no benefit.
- fix: No code change. This is an accurate durable record that three commonly-flagged areas (frames-after-close, renderer hang, backpressure) are already sound. The only optional refinement is wording: the note should say client-bound frames are readyState-gated at TWO sites (sendToClient in _shared.js:60 and the raw upstream-forward in openai.js:75) rather than a single chokepoint, since both carry the WebSocket.OPEN guard. No load-bearing logic should be touched.

## #14 [low/contained] resource-lifecycle
**Benchmark warms a whisper-server that is never stopped after a 'Keep cloud' decision or closing Settings — leaves the model resident in VRAM/RAM for the whole session**
- files: /Users/macmini/dev/voice/src/benchmark-run.js:69-95, /Users/macmini/dev/voice/main.js:1296-1340, /Users/macmini/dev/voice/public/settings.html:315-322
- impact: After running a single on-device speed test and then declining it (or just closing Settings), a whisper-server process keeps the speech model resident in GPU VRAM / system RAM for the entire rest of the session on a machine that is using a cloud engine and will never dictate with it. On the CUDA build this needlessly pins hundreds of MB of VRAM. It self-clears only on app quit or the next benchmark run (which kills the prior server first).
- fix: In applyEnvPatchLive (main.js:1720), after applying the patch, when the committed provider is NOT whisper-local/local, call stopWhisperServer() once. stopWhisperServer (whisper-local.js:272-281) early-returns at line 277 when no server is running, so it is idempotent and safe to call unconditionally on the cloud path — a no-op for cloud-only sessions, and skipped for the "Use on-device" path (which sets provider=local, preserving the warm server). This is the smallest fix; the engine:apply site already knows the chosen provider.

## #15 [low/contained] resource-lifecycle
**The relay HTTP server and WebSocketServer are never closed on quit — relay close() and the http.Server returned by startServer are discarded**
- files: /Users/macmini/dev/voice/main.js:255-257, /Users/macmini/dev/voice/server.js:76, /Users/macmini/dev/voice/realtime-relay.js:123-126, /Users/macmini/dev/voice/main.js:1878-1884
- impact: On a clean Electron quit the process dies and the OS reclaims the listening socket and any open browser WebSockets, so there is no accumulating leak in normal use. The gap matters only if the relay were ever torn down without exiting the process (not a current code path), and it means there is no graceful WS close handshake on shutdown.
- fix: Optional graceful-shutdown nicety, not a bug fix. Capture the handles: return the relay handle out of attachRealtimeRelay's call in server.js (e.g. `const relay = attachRealtimeRelay(server, { model });` and expose it on the resolved object alongside `server`/`port`), and store both the server and relay in module-level vars from bootRelayServer. Then in shutdownAll() add `try { relay?.close(); } catch {}` (closes the WebSocketServer) followed by `try { httpServer?.close(); } catch {}`. Close the WS server FIRST, then the http server. Do NOT await server.close(): it only stops accepting new connections and its callback may never fire while sockets are open — shutdownAll is fire-and-forget synchronous and the process exits immediately after, so a blocking await would risk hanging quit. Alternatively, accept the current behavior and add a one-line comment in shutdownAll noting the OS reaps the socket on exit, making explicit teardown unnecessary. This is NOT intentional decision #5 (that governs where the relay binds — loopback + auto-port — not lifecycle teardown).

## #16 [low/trivial] security-ipc
**engine:apply trusts renderer-supplied modelName for the path written to WHISPER_MODEL (no allow-list / traversal guard)**
- files: main.js:1343-1356
- impact: Real exposure is small: the settings window is the only caller and spawn uses array args (no shell injection), so the worst case is pointing Whisper at an arbitrary existing file the user already has, or persisting a bogus STT_PROVIDER that the relay falls through on. But it is an unvalidated renderer value reaching a persisted config and a child-process arg. Note engine:benchmark is safe — it routes modelName through ensureModel→modelUrl which rejects anything not in the MODELS allow-list (model-download.js:40).
- fix: Two-line hardening with no behavior change for legitimate use. In settings.js, add `export` to VALID_PROVIDERS (currently module-private). In main.js engine:apply (around line 1344-1348): reject unknown providers with `if (!VALID_PROVIDERS.has(provider)) return { error: "Unknown engine." };` and require modelName to be a bare allow-listed key before joining the path: `if (!MODELS[payload.modelName]) return { error: "Unknown speech model." };` (MODELS imported from model-download.js, keys ggml-base-q5_1.bin / ggml-small-q5_1.bin). This also rejects any path-separator/traversal since a basename with `../` is not a MODELS key. Defense-in-depth only — there is no remote-content renderer in this app, so impact is bounded to an already-trusted local window.

## #17 [low/contained] security-ipc
**No CSP meta tag and no setWindowOpenHandler / will-navigate guard on any window**
- files: public/settings.html:173, public/dictionary.html:73, public/vocab-prompt.html:62, public/pill.html, public/splash.html:116, main.js:752-787
- impact: Defense-in-depth only, not currently exploitable: every window already runs with contextIsolation:true, sandbox:true, nodeIntegration:false and loads only local, app-authored HTML, so there is no remote/attacker content path. But if a future change ever renders server- or transcript-derived HTML, the absence of a CSP and of a navigation/window-open guard removes the second layer that would contain it (e.g. a window navigating itself to an external origin, or window.open spawning an un-sandboxed child).
- fix: The web-contents-created handler (deny window.open + block will-navigate to non-loopback/non-file origins) is the high-value half: it's a single small block in main.js, no per-file changes, and cleanly contained — add it. Note the proposed guard must allow the actual loopback URL with the runtime serverPort (it does). The CSP meta tag is more fiddly: every window uses an inline <script>, so a `default-src 'self'` CSP without `'unsafe-inline'` (or per-block hashes/nonce) would BREAK all five windows — the fix as written ("inline-script hashes or a nonce") is correct only if the hashes are actually computed and maintained, otherwise it's a regression. Given there is no current exploit path (the one transcript-derived innerHTML at vocab-prompt.html:76 is already esc()-escaped), the pragmatic minimal fix is: add the navigation/window-open guard now (trivial, safe), and defer CSP unless/until a window starts rendering server- or transcript-derived markup unescaped.

## #18 [low/trivial] crossplatform-ffi
**PowerShell one-liners single-quote file paths that can contain an apostrophe (Windows username), breaking model/binary setup**
- files: src/model-download.js:152-155, src/model-download.js:162-164, src/model-download.js:167-176
- impact: A Windows account whose username contains a single quote (e.g. 'O'Brien' → ...\Users\O'Brien\...) produces a malformed PowerShell command. Expand-Archive/Move-Item fail, ensureWindowsBinaries throws 'whisper-cli.exe not found after extracting', and the guided on-device engine setup dies with a confusing error for that user. Apostrophes in Windows usernames are uncommon but legal and do occur.
- fix: Escape embedded single quotes before interpolating into the single-quoted PowerShell literals: define `const q = (p) => p.replace(/'/g, "''")` and wrap each interpolated path — `Expand-Archive -Path '${q(zipPath)}' -DestinationPath '${q(destDir)}' -Force` and likewise q(nested)/q(binDir) in the Move-Item/Remove-Item line. Doubling the single quote is the correct PowerShell escape inside a single-quoted literal and is a no-op for normal paths, so it's behavior-preserving for every existing user. Severity is low rather than medium because the trigger population (Windows accounts with an apostrophe in the username) is genuinely rare, and the failure is a clean, recoverable setup error rather than data loss or a security breach.

## #19 [low/trivial] crossplatform-ffi
**Typing module is prewarmed only on Windows, so Linux pays the ~300ms nut-js import on the first dictation**
- files: main.js:1843-1845, src/typing.js:56-60, src/typing.js:69-104
- impact: On Linux in the default clipboard mode (and on Windows/macOS only when TYPE_VIA_CLIPBOARD=false), the heavy jimp/nut-js stack is imported on the user's FIRST paste instead of at boot, delaying the first dictation's text by ~300ms. The comment at main.js:1839-1842 claims the cost is paid up front 'when the character-by-character typing path is in use', which is not true on Linux.
- fix: Drop the `if (process.platform === "win32")` wrapper at main.js:1843 and call `import("./src/typing.js").then((m) => m.prewarmTyping()).catch(() => {})` unconditionally. prewarmTyping() already self-skips on native-paste platforms via the `nativePaste` guard (typing.js:57-58), so the only behavioral change is that Linux-from-source and macOS-with-TYPE_VIA_CLIPBOARD=false warm the nut-js stack at boot instead of on first paste. Windows/macOS default clipboard mode is unaffected (still an immediate-return no-op). Also fix the stale comment at main.js:1839-1842, which implies the up-front cost is paid on the char-by-char path — true once the guard is removed, but currently false on Linux.

## #20 [low/trivial] ux-copy
**Pill action button permanently reads "Recording" — a noun that sounds like a record button, never updated to its real action**
- files: /Users/macmini/dev/voice/public/pill.html:129, /Users/macmini/dev/voice/public/pill.html:146, /Users/macmini/dev/voice/public/pill.html:177, /Users/macmini/dev/voice/public/pill.html:208
- impact: On the result pill, the button next to Copy reads "Recording" — a bare noun a user reasonably reads as "start a recording," the opposite of what it does (it opens/plays the clip just captured). It's also inconsistent with the tray menu wording for the identical action, so the same feature is named two different things in two places.
- fix: Change pill.html:129 to `<button id="open" type="button">Play recording</button>` — matches the tray's existing "Play recording"/"Play last recording" wording (main.js:1559, 1586) and reads as the action it performs. Static text, no JS change needed since applyState never sets openBtn.textContent. Severity is low, not high: it's a one-word cosmetic label on a transient result toast — the button works, nothing breaks, no data risk — but it is a genuine clarity/consistency defect worth the one-line fix.

## #21 [low/trivial] ux-copy
**Web-component buttons use Title Case instead of sentence case (humanizer)**
- files: /Users/macmini/dev/voice/public/realtime-voice-agent/template.js:198, /Users/macmini/dev/voice/public/realtime-voice-agent/template.js:199, /Users/macmini/dev/voice/public/realtime-voice-agent/template.js:226, /Users/macmini/dev/voice/public/realtime-voice-agent.js:229, /Users/macmini/dev/voice/public/realtime-voice-agent.js:259, /Users/macmini/dev/voice/public/realtime-voice-agent.js:281
- impact: Title-case button labels are a low-grade AI/UI-template tell and read as machine-generated; they're also out of step with the sentence-case buttons used everywhere else in the same product.
- fix: Sentence-case the four distinct labels. In template.js: line 198 -> "Save personality", line 199 -> "Reset personalities", line 226 -> "Start talking". In realtime-voice-agent.js: line 229 -> "Stop and send", lines 259 and 281 -> "Start talking" (and the log string at :231 "press Stop and Send" should match -> "press Stop and send" for consistency). Pure static-text edits — no logic, identifiers, or control flow touched; the button labels just need to match the sentence-case convention already used everywhere else.

## #22 [low/trivial] ux-copy
**Live status fields in the web voice component are not announced to screen readers (a11y)**
- files: /Users/macmini/dev/voice/public/realtime-voice-agent/template.js:204, /Users/macmini/dev/voice/public/realtime-voice-agent/template.js:207, /Users/macmini/dev/voice/public/realtime-voice-agent/template.js:210, /Users/macmini/dev/voice/public/realtime-voice-agent/template.js:213, /Users/macmini/dev/voice/public/realtime-voice-agent/template.js:219
- impact: For a VOICE component, the entire feedback loop — mic went live, agent is speaking, connection failed — is visual-only. A blind or low-vision user gets no announcement that recording started or that the AI is responding, which is exactly the moment they most need it.
- fix: In template.js, add role="status" aria-live="polite" to the connectionStatus, micStatus, and voiceStatus spans (lines 207, 211, 219) so state transitions are announced; keep levelStatus (line 215) silent or aria-hidden since it updates every few frames and would spam a screen reader. Add aria-pressed to the talk button (line 226) and set this.talkButton.setAttribute("aria-pressed", String(this.isRecording)) wherever isRecording flips (realtime-voice-agent.js:209/255). Pure attribute additions, no logic change. Severity is low rather than medium: this is a developer-facing demo/control panel (Connect/Disconnect/Save Personality/raw event-log <pre>), not the primary Electron dictation UI, so its real-world non-sighted-user audience is small — but the impact is genuine for that audience and the fix is cheap, so it is a worthwhile polish win.

## #23 [low/trivial] ux-copy
**Muted helper text (#6b7986) falls below the WCAG AA contrast floor**
- files: /Users/macmini/dev/voice/public/dictionary.html:36, /Users/macmini/dev/voice/public/dictionary.html:49, /Users/macmini/dev/voice/public/dictionary.html:28, /Users/macmini/dev/voice/public/settings.html:35
- impact: The empty-state message, the dictionary word count, and the input placeholders are hard to read for low-vision users and in bright ambient light — they sit just under the readable threshold.
- fix: Replace #6b7986 with a lighter muted gray at dictionary.html:28, :36, :49 and settings.html:35. The proposed #828f9d clears the floor on the page background (5.4:1) and is borderline on the input background (4.79:1), so it works; for full safety on the input/placeholder cases go one step lighter (e.g. #8d99a7, which clears 4.5:1 on both #14181e and #1d232b). The strongest, clearest wins are the two body-text cases (.empty empty-state message and .count word count) since placeholder contrast is a softer WCAG target. Pure CSS color change, no logic or layout touched.

## #24 [low/trivial] ux-copy
**"Clear recordings" tray item and "Delete all recordings now" settings button delete with no confirmation**
- files: /Users/macmini/dev/voice/main.js:1634, /Users/macmini/dev/voice/main.js:1636, /Users/macmini/dev/voice/public/settings.html:215
- impact: One stray menu/button press permanently deletes every saved recording, including the clip that backs an as-yet-unrecovered failed dictation, with no "are you sure?" and no undo.
- fix: Add a confirmation gate before deletion without touching clearRecordings itself. Tray path: in the click handler at main.js:1636, call `dialog.showMessageBox` (type "warning", buttons ["Delete", "Cancel"], defaultId/cancelId Cancel) and only proceed on the Delete response. Settings path: in settings.html:215, wrap the call in a `confirm("Delete all saved recordings? This can't be undone.")` (or route through the bridge to a main-process dialog). Copy should match the app's plain-English voice, e.g. "Delete all saved recordings? You won't be able to recover a dictation that didn't land." Optional but consistent: apply the same gate to the dictionary delete at dictionary.html:95.

## #25 [low/contained] ux-copy
**Dictionary "Add" gives no feedback when a word is a duplicate or invalid — the input just blanks**
- files: /Users/macmini/dev/voice/public/dictionary.html:101, /Users/macmini/dev/voice/public/dictionary.html:105, /Users/macmini/dev/voice/src/vocab.js:137
- impact: A user who re-adds an existing word, or types something the store rejects, sees the input clear with no entry appearing and no message — it reads as a silent failure or a bug.
- fix: Have the "vocab:add-many" handler (main.js:1248) return { added, terms } instead of just the list, then in addWords() (dictionary.html:101) show a brief inline status near the Add button: "Added N word(s)", "Already in your dictionary", or for the over-length case "Too long — keep it under 40 characters". Keep MAX_TERM_LEN messaging accurate; do not claim a min-length rule since none exists.

## #26 [low/trivial] ux-copy
**On-device engine panel sits idle showing "Checking your computer…" forever if the probe call rejects**
- files: /Users/macmini/dev/voice/public/settings.html:97, /Users/macmini/dev/voice/public/settings.html:251, /Users/macmini/dev/voice/public/settings.html:252
- impact: If the capability probe throws, the panel is stuck on a perpetual "Checking…" message with no error and no recovery, implying work is still happening when it has actually stopped.
- fix: In the catch at settings.html:252, set a fallback before returning instead of swallowing silently, e.g.: `catch { el("engineSuggest").textContent = "Couldn't check this computer — pick a cloud engine above and paste your key below."; return; }`. One-line copy change in the renderer, no behavior or lifecycle risk.

## #27 [low/trivial] ux-copy
**Web-component subtitle reads like a UI instruction sheet, not human guidance (humanizer)**
- files: /Users/macmini/dev/voice/public/realtime-voice-agent/template.js:180
- impact: The very first line a visitor reads on the demo reads like generated step-by-step UI text rather than a person talking, which undercuts a product whose whole pitch is natural conversation.
- fix: Reword the default subtitle to drop "send your voice" and match the real click-toggle control labels. Since the button is a click toggle ("Start Talking" -> "Stop and Send"), NOT press-and-hold, the fix must not say "hold". At template.js:180 use something like: "Pick a personality and connect, then hit Start Talking, say your piece, and click again to send." Reject the finding's own proposed text ("hold the talk button and just speak") — it misdescribes the UI.

## #28 [low/trivial] ux-copy
**Connection-failed state in the web component shows a terse "Mic needed"/"No mic audio" with no recovery guidance**
- files: /Users/macmini/dev/voice/public/realtime-voice-agent.js:195, /Users/macmini/dev/voice/public/realtime-voice-agent.js:171, /Users/macmini/dev/voice/public/realtime-voice-agent.js:238
- impact: In the web component the visible failure is a cryptic two-word tile; the only real explanation is buried in a debug log the user isn't reading. They're told something's wrong but not what to do.
- fix: Surface the recovery guidance in the user-attention zone, reusing the strings already written for the log. Add a one-line visible message element under the action buttons (or widen the relevant status pill) and, in the three failure branches, write the actionable text there instead of (or in addition to) the log: at 194-196 set it to "Microphone blocked — allow mic access for this site, then press Connect/Talk again."; at 238-239 set "No mic audio reached the app — check your browser mic permission and input device."; at 170-176 set "Connection failed — the local voice relay isn't reachable. Make sure the app is running, then Connect again." Pure copy/markup add; no change to relay, FFI, audio, or socket logic.

## #29 [low/trivial] simplification-deadcode
**isRightAltDown() and its VK_RMENU constant are dead — no caller anywhere, and the module header comment falsely claims the hotkey loop uses it**
- files: src/foreground.js:340-342, src/foreground.js:291-292, src/foreground.js:7-8
- impact: An exported function plus a dedicated VK_RMENU constant ship in every build but are never invoked. The header comment misdocuments the module's live API, so a future reader believes right-Alt key polling is wired up when nothing reads it. No runtime effect; pure dead weight and a stale comment.
- fix: Delete isRightAltDown() (src/foreground.js:340-342) and its sole-use VK_RMENU const + comment (src/foreground.js:291-292) — grep proves line 341 is the only consumer of VK_RMENU, so removing the function leaves the const dangling and both should go together. Drop "isRightAltDown / " from the header comment at line 7 so it reads "isRightCtrlDown — synchronous physical-key state queries...". Keep that comment edit minimal; do not expand it to enumerate isCtrlDown/isShiftDown (separate, out-of-scope incompleteness). Leaves the live isRightCtrlDown/isCtrlDown/isShiftDown untouched. Exports are imported by name and no test references the deleted symbols, so blast radius is trivial.

## #30 [low/trivial] simplification-deadcode
**npm run test:cleanup can never fail — the harness prints pass/fail but always exits 0**
- files: scripts/cleanup-test.js, package.json:14, .claude/plan.md:99
- impact: A test script in package.json that returns success even when cleanup cases fail. Anyone (CI, a developer, a pre-commit hook) running `npm run test:cleanup` gets a green exit code while real failures scroll past in the log, so regressions in the LLM cleanup pass slip through unnoticed.
- fix: Append one line after the summary log (after line 121): `process.exitCode = passed === total ? 0 : 1;`. No change to printed output. Note: this harness calls a live LLM (polishTranscript), so output is non-deterministic and a few cases can flake; the gate makes a manual run / pre-commit / future CI fail on regressions, which is the intent. Severity is low (not medium) because this is a dev-only manual harness — no CI wires it, the active package scripts don't run it, and it isn't a runtime path — so today nothing silently regresses in production; it's a latent dev-ergonomics gap the repo already planned to close.

## #31 [low/trivial] simplification-deadcode
**pnpm-workspace.yaml lists electron-winstaller as a build dependency, but it is not a dependency, is not installed, and the build uses electron-builder/NSIS**
- files: pnpm-workspace.yaml
- impact: Dead build configuration. pnpm only runs install scripts for packages that are actually present, so these two entries never do anything — they just imply a Squirrel-based Windows installer that the project does not use, misleading anyone reading the build setup.
- fix: Delete the two electron-winstaller lines from /Users/macmini/dev/voice/pnpm-workspace.yaml: remove `electron-winstaller: true` (line 2) under allowBuilds and `- electron-winstaller` (line 7) under onlyBuiltDependencies. Leave electron/koffi/uiohook-napi (real, installed, native build steps). Zero build/runtime effect since the package isn't installed.

## #32 [low/trivial] simplification-deadcode
**APP_HOME is exported from bootstrap-env.js but consumed nowhere**
- files: src/bootstrap-env.js:42
- impact: An unused public export. The comment above it ('reused by ... the on-demand downloader and main.js') is true for MODELS_DIR/BIN_DIR but not for APP_HOME — nothing imports the raw home path. Harmless but a small piece of dead API surface.
- fix: Delete line 42 `export const APP_HOME = HOME;` in src/bootstrap-env.js. The internal const HOME (line 38) stays and continues to derive MODELS_DIR/BIN_DIR (lines 43-44) and drive the .env migration/PATH/model-resolution logic, so removing only the export changes nothing functional. No callers to update (main.js:50 never imported it). Optional polish: leave the lines 40-41 comment as-is since it reads correctly for the MODELS_DIR/BIN_DIR exports that immediately follow.

## #33 [low/trivial] simplification-deadcode
**ARCHITECTURE.md's list of the web component's public attributes omits the live `title` and `subtitle` attributes**
- files: docs/ARCHITECTURE.md:84, public/realtime-voice-agent.js:69-70, public/realtime-voice-agent/template.js:179-180
- impact: An integrator reading the architecture doc to embed <realtime-voice-agent> won't know they can set the heading and subheading text via `title`/`subtitle` attributes — they're a real, supported part of the drop-in component's API but undocumented.
- fix: Append `title` and `subtitle` to the Public attributes list at docs/ARCHITECTURE.md:84 so it reads "Public attributes: `endpoint`, `agent`, `compact`, `instructions`, `autoconnect`, `title`, `subtitle`." Pure documentation edit, no code change.

## #34 [low/trivial] docs-accuracy
**PORT documented as default 3000, but code defaults to 0 (auto-pick free port)**
- files: SETUP.md:84, SETUP.md:149, docs/ARCHITECTURE.md:127, server.js:29, .env.example:10-13
- impact: The two settings tables and the SETUP 'How it works' prose tell a reader the relay defaults to 3000 and only moves off it on a collision. The real behavior — set by the deliberate auto-free-port fix (commit a1b6f9a) — is that with no PORT set it never uses 3000 at all; it grabs a fresh free port each launch. A user who pins their firewall, docs, or a reverse proxy to 3000 based on these tables will be wrong.
- fix: Doc-only change, no code touched. SETUP.md:84: change the default cell from `3000` to `(free port each launch)` to match the WHISPER_PORT row two lines up (SETUP.md:78). docs/ARCHITECTURE.md:127: change the default cell from `3000` to `(auto: free port)`. SETUP.md:149: reword to "The relay auto-picks a free port each launch (so it never collides with a dev server on 3000); set PORT only to pin a fixed one, and if that pinned port is busy it still falls back to a free one." This aligns all docs with .env.example:10-13 and the intentional auto-free-port behavior.

## #35 [low/trivial] docs-accuracy
**ARCHITECTURE.md file map omits the public/realtime-voice-agent/ submodule that Pass 6 created**
- files: docs/ARCHITECTURE.md:81-92, public/realtime-voice-agent.js:1-12, docs/REFACTOR.md:41
- impact: The file map claims to be a 'snapshot of the current code' (ARCHITECTURE.md:1) but is missing three real source files. A reader navigating by the map won't know the web component is now multi-file.
- fix: Under the public/ section of the ARCHITECTURE.md file map, add a sub-bullet beneath the realtime-voice-agent.js entry (line 84): 'realtime-voice-agent/ — agents.js (personality presets + persistence), audio-utils.js (base64/Int16 PCM helpers), template.js (shadow-DOM markup) — split out of the entry file in refactor Pass 6.' Markdown-only change; no code touched.

## #36 [low/trivial] docs-accuracy
**ARCHITECTURE.md env-var table is missing several env vars the code reads**
- files: docs/ARCHITECTURE.md:123-144, main.js:1054, main.js:1057, main.js:183, src/typing.js:15, src/providers/whisper-local.js:179
- impact: The architecture env table is incomplete versus both the code and SETUP.md. Lower impact than the WHISPER_LANGUAGE gap because SETUP.md covers the user-facing ones; mainly an internal-consistency issue between the two docs.
- fix: Add rows to the ARCHITECTURE.md env table (lines 123-144) for the vars the code reads but the table omits: at minimum RECORDINGS_ENABLED (default true), RECORDING_RETENTION_DAYS (default 7), GVOICE_CORRECTION_WATCH_MS (default 12000), GVOICE_DEBUG (off), and WHISPER_LANGUAGE. The advanced knobs TYPE_PASTE_TIMEOUT_MS (default 4000) and WHISPER_PROMPT can be added or left as code-only. Note that RECORDINGS_ENABLED, RECORDING_RETENTION_DAYS, and WHISPER_LANGUAGE are also missing from the SETUP.md env table (SETUP.md:70-86) — so they should ideally be added there too, since the finding's assumption that SETUP.md already covers them is wrong. Alternatively, add a one-line note that the SETUP.md table is the canonical user-facing list; but since SETUP.md is itself incomplete, the better fix is to fill both tables for the user-facing vars (the three recording/language ones).

## #37 [low/trivial] docs-accuracy
**README/SETUP describe the right-Ctrl tap as 'flip the language' (2-way) but it cycles 3 values**
- files: README.md:15, SETUP.md:3, main.js:794, main.js:809-810
- impact: A user taps right Ctrl expecting it to switch between two languages and is surprised by a third state (auto). Minor wording, but 'flip' undersells a 3-way cycle.
- fix: In README.md:15, replace "Tap **right Ctrl** to flip the language." with "Tap **right Ctrl** to cycle the dictation language (Auto → Croatian → English)." This matches the three human labels the app itself displays at main.js:801-805 and the actual cycle order at main.js:794. SETUP.md needs no change (it does not mention the toggle).

## #38 [low/contained] vocab-cleanup-data
**Custom dictionary store grows unbounded — only the Deepgram URL is capped at 100, not the stored terms or the OpenAI/cleanup prompt**
- files: /Users/macmini/dev/voice/src/vocab.js:137-151, /Users/macmini/dev/voice/src/vocab.js:319-329, /Users/macmini/dev/voice/src/cleanup.js:155-161
- impact: The assignment cites a stated '100 keyterms' cap; in practice only Deepgram is bounded. A user who pastes a large word list into the dictionary manager (vocab:add-many splits on commas/newlines) inflates the OpenAI transcription prompt and the cleanup system/user prompt on every single dictation — adding latency and token cost, and on OpenAI risking prompt bloat that dilutes recognition. Not a crash; a slow, quiet degradation at the tail.
- fix: Cap store.terms in addTerm (e.g. drop the oldest when length would exceed ~200) — a single guard that bounds the persisted store and all three consumers at once, simpler than patching openaiPromptAddition and cleanup's vocabHint separately. Alternatively, mirror deepgramKeyterms and slice(-100) inside openaiPromptAddition() and cleanup.js's vocabHint. Either is purely additive with zero behavior change for normal-sized dictionaries. Optionally bound store.dismissed the same way.

# REFUTED / UNCERTAIN (do not act)
- [resource-lifecycle] refuted: powerMonitor 'resume'/'unlock-screen' listeners are added once but never removed (acceptable, app-lifetime) — confirmed no duplicate registration
    why: The code at main.js:1497-1527 matches the claimed evidence exactly. setupPowerMonitor() sets powerMonitorWired=true and returns early on any re-entry (main.js:1499-1500), so the two listener registrations powerMonitor.on("resume", ...) and powerMonitor.on("unlock-screen", ...) at main.js:1525-1526 execute at most once. grep across main.js confirms setupPowerMonitor() has exactly one caller, bringUpDictation() at main.js:1697, and bringUpDictation() is itself idempotent via dictationBroughtUp (returns early at main.js:1692 before reaching the setupPowerMonitor() call). powerMonitor is imported only once (main.js:6) and referenced nowhere else in the repo. There is no duplicate registration and no leak. This is not a bug — it is a deliberate app-lifetime registration on a tray app, which is the correct pattern (no point off()'ing listeners that must live as long as the process). The finding itself concedes this ("acceptable, app-lifetime — confirmed no duplicate registration", PROPOSED FIX: "No change needed"). There is no defect in the code and nothing actionable; it is a documented non-issue, so as a reported problem it is refuted.
- [security-ipc] uncertain: Deepgram model query param is attacker-controllable and is reflected into the upstream Deepgram URL
    why: The factual claims are all accurate against the real code. realtime-relay.js:101 reads `modelOverride = requestUrl.searchParams.get("model")` and line 104 passes `model: modelOverride || deepgramModel` into attachDeepgram. deepgram.js:40-41 builds the upstream URL via `new URLSearchParams({ model, ... })` (so the value is URL-encoded — no param-injection/SSRF), and deepgram.js:61 keys the keyterm-vs-keywords branch off `/nova-3/i.test(model)`. There is no model allow-list anywhere (grep for allow-list/validateModel/KNOWN_MODELS returns nothing). So the client-supplied `model` param is indeed unvalidated and selects the upstream Deepgram model the user's key is billed for.

What keeps this from being a confirmed standalone bug is the finding's own impact framing. The relay binds 127.0.0.1 only (server.js:85,91) and accepts upgrades based purely on pathname with no Origin check and no per-connection auth token (realtime-relay.js:68-80; grep for token/auth/nonce in the relay returns nothing). The reviewer concedes "On its own (trusted client) it is benign — the value is encoded." The real-world harm ("cross-origin page can force an expensive model") is explicitly predicated on "the missing Origin check above" — a separate finding. But once that gate is open, the attacker already has fully billed access to unlimited dictation/realtime sessions on the victim's key; forcing a pricier Deepgram model is a marginal cost-amplifier, not an independent vulnerability. The reviewer even states it is "largely subsumed if the Origin check lands." So the cited facts are real, but this is a derivative sliver of the Origin-check issue rather than a distinct bug that stands on its own — hence uncertain rather than confirmed.
- [security-ipc] refuted: IPC handlers that drive typing/file actions do not verify event.sender
    why: The cited code is accurate: dictation:transcript (main.js:1089), pill:open (main.js:1218), vocab:add/dismiss (main.js:1235/1240), vocab:add-many/remove (main.js:1248/1258), and settings:save (main.js:1267) all destructure `_event` and never check `event.sender`. preload.cjs:14 exposes sendTranscript as claimed. So the literal observation ("no handler checks the sender") is true. But the claimed PROBLEM — an actual injection/escalation path — does not exist in the current code, and I refute it on three independently sufficient grounds: (1) One preload per window. main.js:303/409/575/683/725/763 attach six distinct preload files, one to each window's webPreferences.preload, and I confirmed each bridge surface lives in exactly one renderer (grep: dictationBridge only in public/dictation.js; pillBridge only in pill.html; settingsBridge only in settings.html; etc.). No renderer can fire a channel it doesn't already legitimately own. (2) All windows load trusted local content only — loadFile of bundled public/*.html or loadURL of http://127.0.0.1:${serverPort}/... served by the loopback-only relay (main.js:316/422/584/691/736/787). No remote origin is ever loaded. (3) Every window runs with contextIsolation:true, nodeIntegration:false, sandbox:true. The reviewer concedes both points themselves: severity LOW, and "today there is no untrusted sender ... becomes a real escalation vector only if a renderer is ever compromised." That is the definition of a hypothetical — the finding is entirely conditional on a second, undemonstrated vulnerability (the "CSP/navigation gap above"). The missing event.sender assertion has zero behavioral effect on the running app. A finding is confirmed only when the real code clearly exhibits the problem; here it exhibits a benign code smell, not an exploitable defect.
- [crossplatform-ffi] refuted: whisper-local / relay default model points at the English-only ggml-small.en file, which the downloader and hr/en logic can never produce or use correctly
    why: The cited facts are accurate but the finding's headline ("can never produce or use correctly") and its impact do not hold up against the code.

What is true: relay default is cwd-relative `"./models/ggml-small.en-q5_1.bin"` (realtime-relay.js:55); whisper-local fallback is file-relative `join(__dirname,"..","..","models","ggml-small.en-q5_1.bin")` (whisper-local.js:295); the downloader's MODELS table only knows the two multilingual files (model-download.js:24-27); recommendedAssets() only returns those two (hardware.js:70-73); transcribePcm forces parallel en+hr legs (whisper-local.js:651-655).

Why it's refuted — there is no path where the `.en` name specifically causes a distinct failure:
1. Packaged app: main.js:5 imports bootstrap-env BEFORE server.js, so bootstrap-env.js:88-97 sets WHISPER_MODEL to the absolute path of whatever the downloader dropped, or leaves it unset → needsOnboarding (main.js:1753-1755) fires "point GVoice at a model". The relay literal is never reached; the downloader/default "disagreement" never surfaces.
2. Packaged with no model anywhere: the literal is reached but the file is missing regardless of its name — renaming to ggml-small-q5_1.bin wouldn't help, that's missing too. Identical failure and onboarding message.
3. npm run dev (`node server.js`, package.json:10) — server.js:2 uses plain `import "dotenv/config"` and does NOT run bootstrap-env. The literal IS reached, but I confirmed `/Users/macmini/dev/voice/models/ggml-small.en-q5_1.bin` is checked into the repo (ls models/) and both default paths resolve to it and exist. English dictation works.

The "use correctly" half of the headline is false: the file exists, is used, and English works. bootstrap-env.js:89 explicitly lists `ggml-small.en-q5_1.bin` as a supported fallback candidate — the codebase endorses the name on purpose, it doesn't "disagree" with it. The hr-leg-on-an-en-model degradation is real but dev-only, and pickTranscript (whisper-local.js:632-634) keeps the better of the two legs, so it self-mitigates.

The proposed fix is counterproductive: the repo ships ONLY the .en model, so renaming the relay/whisper-local default to the multilingual ggml-small-q5_1.bin makes `node server.js` with unset WHISPER_MODEL resolve to a file that isn't in the repo and break dev, unless they also swap the checked-in model.
- [crossplatform-ffi] refuted: wmic-first GPU detection relies on a tool removed by default on current Windows; only the slower PowerShell fallback remains
    why: The code at src/hardware.js:91 does try `execFileSync("wmic", ["path", "win32_VideoController", "get", "name"], { timeout: 4000 })` first, and the comment at :87 does say "wmic is deprecated but still present." So the reviewer described the structure accurately. But the finding's framing ("relies on a tool removed by default") is refuted by the code itself: lines :95-100 are an explicit try/catch fallback the author wrote for exactly this case — the comment continues "fall back to PowerShell's CIM cmdlet if it's been removed." Code that handles the very failure it's accused of failing on is robust by design; it does not "rely on" wmic. Correctness is fully preserved (the reviewer concedes this), and the whole function is wrapped to return "none" rather than throw (:107-109). The IMPACT is also wrong: detectGpu()/probeCapability() is only reached from two user-initiated Settings IPC handlers — main.js:1283 (engine:probe, fired when the user opens the speech-engine panel) and main.js:1305 (engine:benchmark, fired when the user clicks the speed test). There is no recurring "every GPU probe" path and no "main-process freeze" — that framing is borrowed from a different finding. Cost framing is also overstated: a *missing* wmic on 24H2 throws ENOENT near-instantly; the 4000ms timeout only bites a wmic that exists and hangs. So on fresh 24H2 the real cost is a fast spawn-fail plus one PowerShell call during manual setup, once.
- [ux-copy] refuted: "Add to dictionary?" cursor pop-up has no keyboard path (a11y)
    why: The literal observation is accurate: vocab-prompt.html wires only click handlers (line 82 add, line 83 dismiss), with no keydown listener and no .focus() call. But the framing as a fixable a11y gap is wrong, and the proposed fix is provably inert. The vocab pop-up window is created with focusable: false (main.js:568) and shown with showInactive() (main.js:622). A non-focusable window shown inactive never becomes the OS key/focused window, so renderer keydown listeners never receive hardware keystrokes, and DOM button.focus() cannot reroute OS keyboard input — the keys go to whatever app the user is typing into. So the reviewer's "~4 lines" (keydown + focus) would be dead code that never fires. The non-focusability is a deliberate, documented design (main.js:553-557 and preload-vocab.cjs:2-5): the pop-up must never steal the caret from the user's target app. There is also no global-shortcut path (only the two click-fed IPC sends at main.js:1235/1240). The reviewer's comparison is false-equivalence: dictionary.html (lines 106/110/113) and settings are NORMAL focusable windows (main.js:655, 697) — a different window class designed to take focus. The claimed impact is also overstated: adding a word is fully keyboard-accessible via the Manage Dictionary window from the tray, and ignoring the pop-up is an explicitly designed no-op that auto-dismisses after 7s (main.js:625-627), so a keyboard-only user is never blocked.
- [ux-copy] refuted: Settings "Save" button gives no in-flight disabled state; double-clicks fire concurrent saves
    why: The literal code claims are accurate but the load-bearing impact claim is false. settings.html:204-213: the Save handler calls setStatus("Saving…", false) (line 205), awaits window.settingsBridge.save(collect()) (line 207), and never sets saveBtn.disabled — confirmed. settings.html:266-281: the benchmark button does set el("benchBtn").disabled = true (line 269) and = false (line 281) — confirmed contrast. HOWEVER the claimed IMPACT ("two concurrent writes to the same .env file") does not occur. The write is synchronous and atomic per call: writeEnvFile (src/settings.js:110-114) does readEnvFile + applyEnv + writeFileSync, all sync, with no awaits. The IPC handler ipcMain.handle("settings:save") (main.js:1267-1276) runs on the single main-process event loop, so two queued invokes execute sequentially, not concurrently — there is no read-modify-write interleaving and no torn file. The writes are also idempotent: both rapid clicks call collect() against unchanged DOM inputs, producing identical payloads. applyEnvPatchLive (main.js:1720-1736) is likewise idempotent on a repeat (provider already updated, so reloadDictationWindow won't re-fire). Tellingly, the maintainers added a SERVER-SIDE single-flight guard for the genuinely dangerous operation — engine:benchmark (main.js:1296-1304) — with the explicit comment that "the Settings window's disabled-button guard dies with the window" and a second run would corrupt the .part download. They left settings:save unguarded by design because re-writing identical settings is harmless. The finding's premise (a data hazard) is refuted; only a cosmetic "button doesn't look busy" nit remains.
- [docs-accuracy] refuted: RELAY_PROTOCOL labels the language selector 'Deepgram only', but whisper-local also honors WHISPER_LANGUAGE
    why: I read all four cited lines plus the relay's actual language-routing and the Deepgram provider. The finding's data points are individually true but its conclusion is wrong.

The relay reads the `?language=` query param ONLY inside the `provider === "deepgram"` branch: realtime-relay.js:94 gates on deepgram, line 100 reads `searchParams.get("language")`, line 105 passes it to attachDeepgram. The whisper-local branch (realtime-relay.js:110-112) passes only `{ bin, model }` — it never touches the language param. whisper-local instead sources language from `process.env.WHISPER_LANGUAGE` (whisper-local.js:474). dictation.js:169 builds the whisper-local URL with no language param. All confirmed.

But the doc sentence at RELAY_PROTOCOL.md:13 is the entry for the `language` QUERY PARAMETER (it sits under the "Query parameters" heading, line 10). "Deepgram only" correctly scopes the *query param* — which is exactly what the finding itself concedes is true. The parenthetical "(the default, also via WHISPER_LANGUAGE env)" describes how Deepgram derives its `auto` default, and deepgram.js:29 literally reads `language || process.env.WHISPER_LANGUAGE || "auto"` — so for Deepgram the parenthetical is accurate, not a conflation. The "parallel per-language legs" clause is genuinely Deepgram-only too (deepgram.js:30 AUTO_LANGUAGES; whisper-local runs no legs). So every clause in the sentence is correct for the query param it documents.

The finding's actual charge — that the parenthetical "conflates the query param with the env var, which is NOT Deepgram-only" and misleads a reader into thinking "whisper-local has no language control" — is not substantiated. The doc never claims whisper-local lacks language control; it is simply silent on whisper-local language here. The only real residue is a doc *omission* (WHISPER_LANGUAGE for whisper-local is documented nowhere — grep finds it only on this one line), not the conflation *error* the finding alleges. The proposed fix mischaracterizes correct text as wrong.
- [vocab-cleanup-data] refuted: settings.js corrupts a CRLF .env: every key gets duplicated and the user's change is silently ignored
    why: The mechanical kernel is real but the headline impact is empirically false. parseEnv splits on "\n" only (settings.js:34) and the matcher /^\s*([A-Za-z_][A-Za-z0-9_]*)\s*=(.*)$/ (settings.js:38) does not match lines carrying a trailing \r, so on a CRLF .env the index is empty and applyEnv (settings.js:80-88) appends instead of editing in place. I reproduced this exactly: parseEnv("STT_PROVIDER=openai\r\nPORT=3000\r\n") returns index keys [], and applyEnv(crlf,{STT_PROVIDER:"deepgram"}) returns "STT_PROVIDER=openai\r\nPORT=3000\r\nSTT_PROVIDER=deepgram\n".

But the load-bearing IMPACT claim — "dotenv reads the FIRST occurrence, so the user's change has no effect at runtime" — is wrong. The runtime loads via bootstrap-env.js:11/72 using dotenv 16.6.1, and dotenv resolves duplicate keys to the LAST occurrence, not the first. I confirmed: dotenv.config() on the buggy CRLF file yields process.env.STT_PROVIDER === "deepgram" (the user's NEW value), and dotenv.parse("A=first\nA=second") returns A="second". Since the bug appends the new value at the END, the user's change DOES take effect. Settings do apply.

The "grows a duplicate block on every Save" claim is also false. The appended line is written with "\n" (applyEnv joins with "\n", line 90), so on the NEXT save parseEnv matches it and edits it in place. Three consecutive saves leave exactly 2 STT_PROVIDER lines, not 3 — it stabilizes at one stale orphan per managed key, it does not grow. Also dotenv strips trailing \r from values (OPENAI_API_KEY=sk-abc\r reads back "sk-abc"), so no value corruption.

What IS real: a CRLF .env ends up with one stale, orphaned duplicate line per managed key (e.g. an old STT_PROVIDER=openai lingering above the live deepgram line), violating the module's stated surgical-in-place-edit contract (settings.js:6-9, 24-28). That is a cosmetic/hygiene defect, not a functional one.
- [vocab-cleanup-data] refuted: settings.js edits the wrong line when a key appears twice in .env
    why: The code mechanics are real but the impact and fix rest on a false premise about dotenv. CONFIRMED parts: parseEnv (src/settings.js:44) does `index.set(m[1], { value, lineIndex })` in a forEach, so a duplicate key overwrites the index entry, leaving it pointing at the LAST occurrence. applyEnv (src/settings.js:80-82) rewrites only `lines[existing.lineIndex]`. My end-to-end test confirms applyEnv('STT_PROVIDER=openai\nPORT=3000\nSTT_PROVIDER=deepgram\n', {STT_PROVIDER:'whisper-local'}) leaves line 0 as openai and updates only line 2 to whisper-local. REFUTING evidence — the central impact claim is wrong: the reviewer asserts 'dotenv loads the first' so saved vs. effective values disagree. That is false for a single file. I tested the actual installed dotenv 16.6.1 (used via src/bootstrap-env.js:11,72 which loads exactly ONE file): dotenv.parse('STT_PROVIDER=openai\n...\nSTT_PROVIDER=deepgram') returns 'deepgram' — LAST-wins. dotenv's parse loop (node_modules/dotenv/lib/main.js:22-44) plainly does obj[key]=value on every match, so within one file the last assignment wins. After applyEnv's edit, dotenv.parse(out).STT_PROVIDER === 'whisper-local' — i.e. dotenv loads exactly the line applyEnv updated. There is NO disagreement between displayed/saved and effective value. The README 'first value wins' text (README.md:309,339) is scoped to combining MULTIPLE files / pre-existing process.env, not duplicate keys within one file, so it does not apply here. The PROPOSED FIX is actively harmful: `if (!index.has(m[1])) index.set(...)` would make settings.js edit the FIRST occurrence while dotenv loads the LAST — that INTRODUCES the split-brain the finding claims to prevent. The current last-occurrence behavior is correct. Residual: a stale duplicate line lingers after editing a hand-duplicated key, but it is cosmetic and harmless since dotenv reads the updated (last) line. The scenario also only arises from a hand-edit — this module never writes duplicates — and the claimed compounding 'CRLF-duplication bug above' is a separate, here-unverified finding.
- [vocab-cleanup-data] refuted: correctTranscript can rewrite a word INSIDE another known word because the onset/known guards run on the matched token, not the position
    why: The FINDING headline is a robustness claim: correctTranscript "can rewrite a word INSIDE another known word because the onset/known guards run on the matched token, not the position." That bug does not exist in the code, and the reviewer's own EVIDENCE concedes it ("I verified 'Cloud-based' and 'Cloud's' with term 'Claud' are correctly LEFT ALONE ... so the guards hold here").

Verified in code: WORD_RE (vocab.js:184) `/[A-Za-zÀ-ɏ][A-Za-zÀ-ɏ'’-]*/g` matches a hyphen/apostrophe token as one unit, so correctTranscript (vocab.js:225-240) sees the whole token "Cloud-based"/"Cloud's", not a sub-slice. The token is then guarded by (a) the known-word check at line 227 and (b) the Levenshtein cap at line 234 (dist 1-2 AND <= floor(maxLen*0.25)). What actually protects "Cloud-based" is the distance cap, NOT the token-vs-position mechanism the headline blames — so the stated CAUSE is also wrong, not just the conclusion.

The only substantiated observation is the NARROWER one buried in IMPACT: multi-word terms are inert in correctTranscript. I confirmed this empirically. normalize() (vocab.js:49-54) preserves the internal space, so addTerm("New Relic") (vocab.js:137-151, MAX_TERM_LEN=40, collapses to single spaces but keeps them) stores key "new relic". Each WORD_RE token has no space, so levenshtein(token, "new relic") is at least the length of the unmatched word + 1: lev("new","new relic")=6, lev("relic","new relic")=4, both far above the cap of 2. So a space-containing key can never match a single token. True — but the finding ITSELF demotes this to "worth a one-line doc note rather than a logic change," i.e. not the robustness bug the headline asserts.

Two inaccuracies in the finding's framing: (1) "dead weight" is overstated — multi-word terms are NOT dead weight; they actively feed deepgramKeyterms() (vocab.js:319) and openaiPromptAddition() (vocab.js:325). They are inert in exactly ONE path (local-Whisper correctTranscript), not generally. (2) The headline's causal mechanism is wrong as noted above. Scoring the FINDING as written: refuted.
- [vocab-cleanup-data] refuted: History/recording paths persisted as absolute strings are never validated against home migration
    why: Every code claim in the finding is accurate, but the finding asserts no actual defect — it self-describes as "NOT a crash or data-loss bug" with "No change required." Verified: history.js:33 stores recordingPath as a literal absolute string with no rewriting (`recordingPath: typeof e.recordingPath === "string" ? e.recordingPath : null`). main.js:1551 guards playback with `const hasRecording = !!entry.recordingPath && existsSync(entry.recordingPath)`. bootstrap-env.js:50-59 migrates only the .env (not history.json or recordings) from LEGACY_HOME to userData on first packaged boot. I checked ALL persisted-path consumers (grep): line 1551 hasRecording guard, line 1561 click handler (gated by hasRecording AND wrapped in `.catch(() => {})`), line 1570 lastRecording (existsSync-filtered), line 1589 (uses lastRecording.recordingPath which is already existsSync-filtered). There is no unguarded consumer of a persisted/migrated path. A stale path after home migration degrades to "Recording unavailable" (line 1559) — exactly the benign behavior the finding admits. Since the finding itself concludes there is nothing to fix and lists it "for completeness," there is no robustness problem to confirm; the graceful-degradation design is already correct.