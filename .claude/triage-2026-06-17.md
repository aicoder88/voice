# Triage — 39 confirmed findings (2026-06-17)

Severity: 0 critical, 0 high, 7 medium, 32 low. (Mature, heavily-reviewed app.)
Rule: fix freely = trivial/contained blast radius + clear win. Surface = load-bearing
internals I can't fully test, or a design decision that's the user's to make.

## WILL FIX — zero runtime risk (docs / config / dead-code / copy)
- #4  .env.example CLEANUP_ENABLED=false → true (copying the example silently disabled cleanup)
- #5  WHISPER_LANGUAGE missing from SETUP + ARCHITECTURE settings tables → add rows
- #6  SETUP 'Languages' overstates auto-detect (auto = hr+en only) → reword
- #33 ARCHITECTURE web-component attrs omit title/subtitle → add
- #34 PORT documented 3000, actually auto-free-port → fix 3 doc spots
- #35 ARCHITECTURE file map omits public/realtime-voice-agent/ submodule → add
- #36 ARCHITECTURE env table missing several vars → add user-facing ones
- #37 README right-Ctrl "flip the language" → "cycle (Auto→Croatian→English)"
- #29 dead isRightAltDown()+VK_RMENU in foreground.js → delete + fix header comment
- #30 test:cleanup always exits 0 → add process.exitCode
- #31 pnpm-workspace.yaml dead electron-winstaller lines → delete
- #32 unused APP_HOME export in bootstrap-env.js → delete line
- #20 pill "Recording" button → "Play recording" (matches tray wording)
- #21 web-component Title Case → sentence case
- #22 web-component status fields → aria-live (attribute-only a11y)
- #23 muted helper text #6b7986 below WCAG AA → lighter gray
- #26 on-device panel stuck "Checking…" on probe reject → fallback message
- #27 web-component subtitle reword (humanizer)
- #28 web-component connection-failed terse → recovery guidance
- #12 RELAY_PROTOCOL raw-binary row doesn't match JSON-only reality → doc-only fix

## WILL FIX — contained, low risk, real win
- #7  renderer terminal guards don't cross-check → one-line early-return (mirrors existing pattern); prevents paste-after-error / error-after-paste
- #9  dictation:error/mic-warning call done() without finalize() → add finalize() (symmetry; clears stray 25s timer)
- #10 Deepgram 'connected' frame twice in auto mode → connectedSent guard (cosmetic)
- #16 engine:apply trusts renderer modelName → allow-list guard (defense-in-depth, 2 lines)
- #17 no navigation/window-open guard → add web-contents-created deny block (CSP deferred per verifier)
- #18 PowerShell apostrophe in model-download paths → q() escape (Windows-only, no-op for normal paths)
- #19 typing prewarm Windows-only → drop platform guard (prewarm self-skips on native-paste; Linux benefit)
- #24 delete-recordings with no confirm → add confirm dialog (data-loss safety)
- #25 dictionary Add no feedback on dup/invalid → return {added} + inline status
- #38 dictionary store unbounded → cap terms in addTerm (additive, no behavior change for normal sizes)

## WILL FIX — medium severity, careful (test where possible)
- #0  bad OpenAI key never surfaces (DEFAULT provider!) → dictation.js: on local.status closed
       w/ code!==1000 & no terminal event seen, reportFailure("check your OpenAI key").
       BULLETPROOF guard required (wrong → spurious error on every good dictation).
- #1  whisper-local ensureWhisperServer ignores model on cache hit → track whisperServerModel,
       restart on mismatch. Testable via parity (whisper-local runs here).
- #14 benchmark warms server never stopped on 'keep cloud' → stopWhisperServer() on cloud path in applyEnvPatchLive.
- #3  sync wmic/PowerShell GPU probe blocks main process (incl. Windows hotkey poll) →
       make detectGpu/probeCapability async; await at 2 IPC sites. Windows-only benefit,
       can't test on macOS → decide async-vs-memoize at impl after reading hardware.test.js.

## WILL SURFACE — user's call / don't silently change
- #2  relay has no Origin/token check → any local webpage can burn your OpenAI credits.
       REAL (default provider spends money). But fix sits on the connection-accept path
       (mis-plumb → ALL dictation breaks) and a mandatory token breaks the reusable
       web-component's cross-origin design. Two options: Origin allow-list (smaller) vs
       per-launch token (stronger). ASK.
- #8  worklet tail-flush — verifier: proposed fix doesn't work, reorders load-bearing commit
       lifecycle for ~42ms of post-release silence already covered by the 450ms tail. NOTE only.
- #11 losing Deepgram leg not closed — low payoff, lifecycle risk if winner closed. NOTE only.
- #15 relay/WS not closed on quit — OS reaps socket on exit; no real leak. NOTE only.

## REFUTED (12) — do not act. Notable kills:
- "settings.js corrupts CRLF .env / drops your change" — empirically false (dotenv last-wins; change applies).
- "settings.js edits wrong duplicate line" — proposed fix would INTRODUCE the split-brain. Current behavior correct.
- vocab pop-up a11y keyboard path — pop-up is focusable:false by design; proposed fix is dead code.
- powerMonitor listener leak — single registration, app-lifetime, correct.
- wmic-removed-on-24H2 — code already has the PS fallback the finding says is missing.
